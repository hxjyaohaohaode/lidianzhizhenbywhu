# Checklist

- [x] `createPortal` is imported from `react-dom` in `src/web/App.tsx`.
- [x] `.chart-tooltip` in `EntHome` is rendered using `createPortal` to `document.body`.
- [x] `.chart-modal-overlay` in `EntHome` is rendered using `createPortal` to `document.body`.
- [x] `.chart-tooltip` in `InvHome` is rendered using `createPortal` to `document.body`.
- [x] `.chart-modal-overlay` in `InvHome` is rendered using `createPortal` to `document.body`.
- [x] `.ndo` (`MemoryNodeDialog`) is rendered using `createPortal` to `document.body`.
- [x] All tooltips and modals position themselves correctly relative to the viewport.