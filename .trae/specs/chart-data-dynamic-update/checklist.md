# 图表动态数据更新检查清单

## 企业端首页图表数据验证

- [x] `zebraRows`（成本结构斑马表）的数据基于 `currentCost` 和 `currentManufacturingExpense` 动态计算，不再使用固定 62%/14%/16%/8%
- [x] `treeRows`（成本分解树状表）的 metric 值反映真实的成本占比计算
- [x] `pivotRows`（多维交叉透视表）的毛利率、产销匹配度等值从输入数据派生
- [x] `boxPlotChart`（箱线图）的 min/q1/median/q3/max 基于用户输入的毛利率动态计算，而非完全硬编码
- [x] `scatterChart`（散点图）的 x/y 坐标从 `quarterSeries` 营收和毛利率数据派生
- [x] `sankeyChart`（桑基图）的流量值与成本结构数据关联，满足流量守恒
- [x] `heatmapChart`（热力图）的硬编码固定值（如 88/92/78 等）替换为动态计算
- [x] `calendarEntries`（日历视图）的 label/value/detail 与当前经营状况关联
- [x] 所有企业端首页图表的 `detail` 和 `note` 字段包含数据来源说明
- [x] 企业端首页图表数据与用户输入的毛利率、营收、成本等保持数学一致性

## 投资端首页图表数据验证

- [x] `boxPlotChart`（行业风险箱线图）的数据从行业基准派生
- [x] `scatterChart`（风险收益散点图）的公司坐标有明确的计算依据或标注为经验估算
- [x] `sankeyChart`（资金流向桑基图）的流量与用户投资偏好关联
- [x] `bubbleChart`（投资三维气泡图）的景气/盈利/市值数据有来源说明
- [x] `heatmapChart`（风险收益热力图）的单元格值从行业基准派生，而非固定偏移量
- [x] 所有投资端首页图表的 `detail` 字段区分"真实数据"和"经验估算"

## 投资分析页图表数据验证

- [x] `heatmapRows`（风险收益热力矩阵）的值从数学模型输出派生，非固定数组
- [x] `pivotRows`（多情景透视矩阵）的情景值基于模型预测，非固定描述
- [x] `sparkRows`（信号趋势表）的趋势数据基于历史会话或合理计算逻辑
- [x] 分析页所有图表的 insight 说明与数学模型输出一致

## 数据说明文档验证

- [x] 所有图表 widget 包含完整的 `detail` 字段，说明计算来源
- [x] 所有图表 widget 包含完整的 `note` 字段，说明业务含义
- [x] `sourceMeta` 明确区分"用户输入"、"行业基准"、"模型计算"、"经验估算"四类数据源
- [x] 图表的数据说明在用户输入变化时能够自动更新

## 数据一致性验证

- [x] 同一数据集在不同图表中的表现逻辑一致（如 radarChart 和 heatmapChart 的维度得分一致）
- [x] 图表数据与指标卡片（metricCards）显示的数值一致
- [x] 图表的 status 标签（good/watch/risk）与数值判断逻辑一致
- [x] 行业基准数据在所有图表中使用统一的 `getIndustryStandard()` 值

## LLM 洞察功能验证

- [x] LLM 洞察生成接口接收的是真实的图表数据作为上下文
- [x] LLM 生成的分析不包含编造的虚假数据点
- [x] UI 上明确标识"AI 生成分析"与"原始数据"的区别
- [x] LLM 洞察在图表数据更新时能够重新生成

## 整体验证

- [x] 输入不同的企业数据后，所有图表数据能够相应变化
- [x] 图表的数据说明文字能够反映当前的实际数据来源
- [x] 无任何图表使用与用户输入无关的硬编码固定值
- [x] 企业端首页功能正常，无渲染错误
- [x] 投资端首页功能正常，无渲染错误
- [x] 投资分析页功能正常，无渲染错误
- [x] TypeScript 类型检查通过，无编译错误
