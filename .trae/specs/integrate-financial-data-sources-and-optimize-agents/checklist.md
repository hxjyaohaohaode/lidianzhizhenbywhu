- [x] 上交所、深交所、北交所定期报告连接器均已具备抓取与解析实现，且自动化验收已覆盖深交所、上交所、北交所三类链路。
- [x] 东方财富个股研报与行业研报接口均已接入，自动化验收覆盖个股研报与行业研报解析链路。
- [x] 宏观数据链路已调整为“公开网页 RAG 默认提供宏观证据，`NBS_COOKIE`、`NBS_TOKEN` 与 `NBS_ACCOUNT + NBS_PASSWORD` 作为可选结构化增强模式”，缺失 NBS 凭证时不会阻塞主工作流。
- [x] RAG 文档口径已与实际实现对齐：当前提供带 citation 追溯的混合排序检索，不宣称外部向量库能力。
- [x] DeepSeek、GLM、Qwen 的验收口径已区分 `passed`、`failed`、`unavailable`，并明确 mock 通过不等同于真实连通通过。
- [x] 最小部署口径已统一为“至少一个模型 API Key 为必需；宏观证据默认由公开网页 RAG 提供，NBS 凭证（Cookie / Token / 账号密码）仅为可选增强”。
- [x] 各智能体（数据获取、分析、决策、交互）协同工作顺畅，实现闭环工作流。
- [x] 极简配置验证通过：已提供 `GET /api/health/ready` 与最小部署审计报告，验证“至少一个模型 API Key 为必需、NBS 凭证可选”时主工作流与个性化记忆链路可运行。
- [ ] 2026-04-05 真实环境复核已确认模型 API 可被读取，最小部署主流程可运行；但国家统计局在 `NBS_COOKIE` 模式下访问 `QueryData` 仍返回 `403`，因此 live 环境仍不能证明 NBS 结构化增强链路已打通。
- [ ] 2026-04-05 金融 RAG 真实质量未达标：`POST /api/rag/realtime` 与 `GET /api/acceptance/competitive-baseline` 显示 `ragTraceability.passedScenarioCount=0/3`，并出现 `Key West Hotels` 等明显误召回，不能宣称金融证据链路通过。
- [ ] 2026-04-05 外部数据源真实链路仍未完全达标：已完成官网页兜底和 URL 优先解析，默认宏观证据也已切换为公开网页 RAG；但公开网页 RAG 的真实质量和外部源 live 证据仍需继续复核，NBS 在 `NBS_COOKIE` 模式下依旧返回 `403`，仅保留为未通过的可选增强项。
- [ ] 当前上线口径应维持“模型与主工作流可运行，具备自动化与降级闭环，宏观证据默认可由公开网页 RAG 提供，但尚未达到真实环境高竞争力验收标准”；在 RAG 真实质量与外部源 live 复核达标前，不得宣称整体性能与响应效果优异。
- [x] 2026-04-05 已完成代码层补救：`env.ts` 支持 `.env` 缺失时回退加载 `.env.example`，避免本地或新环境因遗漏 `.env` 直接启动失败。
- [x] 2026-04-05 已完成连接器兜底增强：SSE、BSE、东方财富在接口空结果或异常时，会继续尝试官网 HTML 页面解析，并补充了自动化测试覆盖。
- [x] 2026-04-05 已完成 RAG 收紧：增加金融/官方源优先、离题结果过滤与更聚焦的查询词，目标是降低 `Key West Hotels` 一类误召回。
- [x] 2026-04-05 已完成本地回归：执行 `npm run test` 与 `npm run typecheck`，确认本次代码改造在本地通过回归校验。
- [ ] 以上改造仅代表代码层整改完成，不代表真实模型、真实外部源与真实业务质量已通过 live 复核；在重新执行 live 验收前不得勾选 Task 10 完成。
