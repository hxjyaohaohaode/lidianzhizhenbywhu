# 系统稳定性与功能完整性修复 - 验收检查清单

## 一、前端UI稳定性

### CSS变量完整性
- [ ] **UI-001**: 暗色主题下`var(--t4)`正确解析为弱化文字颜色（非黑色）
- [ ] **UI-002**: 暗色主题下`var(--glass)`正确解析为半透明玻璃背景色
- [ ] **UI-003**: 暗色主题下`var(--line)`正确解析为边框颜色
- [ ] **UI-004**: 暗色主题下`var(--glass-soft)`正确解析为柔和玻璃背景色
- [ ] **UI-005**: 暗色主题下`var(--rs)`正确解析为小圆角值
- [ ] **UI-006**: 亮色主题下以上5个变量均正确解析
- [ ] **UI-007**: 所有使用`var(--t4)`的21处文字在两种主题下均可见且颜色合理
- [ ] **UI-008**: 所有使用`var(--line)`的12处边框在两种主题下均正常显示

### 跑马灯样式
- [ ] **UI-009**: 跑马灯涨跌颜色统一为涨红跌绿（中国市场惯例）
- [ ] **UI-010**: 跑马灯动画流畅，无样式冲突

### API调用健壮性
- [ ] **UI-011**: SSE流式数据中包含非JSON行时，前端不崩溃，跳过该行继续处理
- [ ] **UI-012**: 服务端返回2xx但非JSON响应时，前端抛出友好错误而非SyntaxError

### 其他前端修复
- [ ] **UI-013**: `splitMode`为true时，`.iwb-split`样式生效，布局产生可见变化
- [ ] **UI-014**: 聊天消息中的HTML特殊字符被正确转义，无XSS风险
- [ ] **UI-015**: 无未使用的类型导入产生TypeScript编译警告

## 二、GMPS模型打分逻辑

### 打分方向正确性
- [ ] **GMPS-001**: `indVol = 0.5`（高波动）时得分≥60分（高风险）
- [ ] **GMPS-002**: `indVol = 0.15`（低波动）时得分≤40分（低风险）
- [ ] **GMPS-003**: `mfgCostRatio = 0.85`（高占比）时得分≥60分（高风险）
- [ ] **GMPS-004**: `mfgCostRatio = 0.5`（低占比）时得分≤40分（低风险）
- [ ] **GMPS-005**: `gpmYoy = 0.15`（毛利率上升15%）时得分为低风险
- [ ] **GMPS-006**: `gpmYoy = -0.15`（毛利率下降15%）时得分为高风险
- [ ] **GMPS-007**: GMPS所有10个特征打分方向与业务语义一致

### 单元测试
- [ ] **GMPS-008**: GMPS打分方向相关单元测试全部通过

## 三、DQI模型计算逻辑

### OCF方向保留
- [ ] **DQI-001**: 当期OCF为正、基期OCF为正时，`ocfRatioChange`正确反映变化方向
- [ ] **DQI-002**: 当期OCF为负、基期OCF为正时，`ocfRatioChange < 1`（反映恶化）
- [ ] **DQI-003**: 当期OCF为正、基期OCF为负时，`ocfRatioChange > 1`（反映改善）

### 成长能力计算
- [ ] **DQI-004**: `baselineGrowth`不再硬编码为0，可从输入获取
- [ ] **DQI-005**: `baselineGrowth`缺失时使用推算逻辑，并在输出中标注数据来源

### 驱动因素识别
- [ ] **DQI-006**: 所有比率接近1（稳定状态）时，驱动因素标识为"无明显驱动"或类似

### 单元测试
- [ ] **DQI-007**: DQI相关单元测试全部通过

## 四、智能体与数学模型集成

### 降级事件可见性
- [ ] **AGENT-001**: DQI计算降级时，`mathAnalysis`智能体status为"degraded"
- [ ] **AGENT-002**: DQI计算降级时，工作流`degradationTrace`包含对应事件
- [ ] **AGENT-003**: GMPS计算降级时，同上
- [ ] **AGENT-004**: DQI和GMPS均成功计算时，`mathAnalysis`智能体status为"completed"

### 数据来源标注
- [ ] **AGENT-005**: DQI使用推算数据时，输出包含`dataProvenance`字段标注推算来源
- [ ] **AGENT-006**: GMPS使用推算数据时，输出包含`dataProvenance`字段标注推算来源
- [ ] **AGENT-007**: `dataProvenance`信息可传递到前端展示

### GMPS触发条件
- [ ] **AGENT-008**: `investmentRecommendation`模式下GMPS正常触发计算
- [ ] **AGENT-009**: 碳酸锂价格优先从数据采集结果获取，非硬编码默认值

## 五、类型定义与治理一致性

- [ ] **TYPE-001**: `DQIResult`包含`governance`字段，与其他模型结果类型一致
- [ ] **TYPE-002**: `MathAnalysisOutput.dqiModel`类型引用`DQIResult`而非内联定义
- [ ] **TYPE-003**: `MathAnalysisOutput.gmpsModel`类型引用`GMPSResult`而非内联定义
- [ ] **TYPE-004**: DQI的`trend`字段为结构化`TrendAssessment`类型
- [ ] **TYPE-005**: TypeScript编译0错误

## 六、系统整体验证

- [ ] **SYS-001**: 后端服务正常启动，无启动错误
- [ ] **SYS-002**: 前端服务正常启动，无编译错误
- [ ] **SYS-003**: 企业端诊断流程端到端可完成，返回包含DQI/GMPS结果
- [ ] **SYS-004**: 投资端分析流程端到端可完成
- [ ] **SYS-005**: 数学模型结果在最终输出中占主导地位，AI仅作为辅助解释
- [ ] **SYS-006**: 所有现有单元测试通过
- [ ] **SYS-007**: 界面无移位、无加载失败、无样式异常
