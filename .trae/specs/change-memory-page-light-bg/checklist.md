# Checklist
- [x] 确保 `public/images/blue-sky.jpg` 被代码正确引用，并能作为浅色模式下记忆页面的背景。
- [x] `MemoryBackgroundCanvas` 在浅色模式 (`!isDark`) 下，移除了原有的线性渐变背景和冲突的云朵绘制，使底层背景图片能够正常透出，并保留了鼠标指针的光晕。
- [x] 在深色模式 (`isDark`) 下，背景仍然保留原有动画效果（星空等），不受新背景图逻辑影响。
- [x] 深浅色模式切换过程平滑无明显异常。
